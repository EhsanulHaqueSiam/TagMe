---
phase: 02-rides-matching
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - lib/features/rides/presentation/screens/ride_detail_screen.dart
  - lib/features/rides/presentation/screens/join_requests_screen.dart
  - lib/features/rides/presentation/widgets/route_visualization.dart
  - lib/features/rides/providers/ride_providers.dart
  - lib/app/router.dart
autonomous: true
requirements: [RIDE-07]

must_haves:
  truths:
    - "Student can view full ride details with poster info, route card, fare estimate"
    - "Student can send a join request on someone else's ride"
    - "Student sees 'Request Pending' if they already sent a request"
    - "Student sees 'You're In' if their request was accepted"
    - "Ride poster can view pending join requests"
    - "Ride poster can accept a request (increments filledSeats atomically)"
    - "Ride poster can decline a request"
    - "Full ride shows 'Ride Full' with no join button"
  artifacts:
    - path: "lib/features/rides/presentation/screens/ride_detail_screen.dart"
      provides: "Full ride detail view with join/status actions"
      contains: "class RideDetailScreen"
    - path: "lib/features/rides/presentation/screens/join_requests_screen.dart"
      provides: "Join request list for ride poster"
      contains: "class JoinRequestsScreen"
    - path: "lib/features/rides/presentation/widgets/route_visualization.dart"
      provides: "Vertical origin-to-destination dots+line component"
      contains: "class RouteVisualization"
  key_links:
    - from: "lib/features/rides/presentation/screens/ride_detail_screen.dart"
      to: "lib/features/rides/data/repositories/join_request_repository.dart"
      via: "send join request and check existing request"
      pattern: "joinRequestRepositoryProvider"
    - from: "lib/features/rides/presentation/screens/join_requests_screen.dart"
      to: "lib/features/rides/data/repositories/join_request_repository.dart"
      via: "accept/decline via Firestore transaction"
      pattern: "acceptRequest"
    - from: "lib/features/rides/presentation/screens/ride_detail_screen.dart"
      to: "lib/features/rides/presentation/widgets/route_visualization.dart"
      via: "route card section"
      pattern: "RouteVisualization"
---

<objective>
Build the ride detail and join request flow: RideDetailScreen with poster info, route card, fare estimate, and context-aware action button (join/pending/accepted/full/own ride), RouteVisualization reusable widget, and JoinRequestsScreen where ride posters accept or decline requests.

Purpose: This completes the matching cycle. Students can view ride details and join rides; posters can manage who rides with them. Addresses RIDE-07.

Output: 2 screens, 1 reusable widget, updated providers, router wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-rides-matching/02-CONTEXT.md
@.planning/phases/02-rides-matching/02-UI-SPEC.md
@.planning/phases/02-rides-matching/02-01-SUMMARY.md
@.planning/phases/02-rides-matching/02-02-SUMMARY.md
@.planning/phases/02-rides-matching/02-03-SUMMARY.md

<interfaces>
<!-- From Plans 01-03 -->

From lib/features/rides/data/models/ride.dart:
```dart
@freezed abstract class Ride with _$Ride { /* all fields */ }
```

From lib/features/rides/data/models/join_request.dart:
```dart
@freezed abstract class JoinRequest with _$JoinRequest {
  const factory JoinRequest({
    String? id, required String rideId, required String requesterId,
    required String requesterName, required String requesterUniversity,
    String? requesterPhotoUrl, required String requesterGender,
    @Default('pending') String status, DateTime? createdAt, DateTime? updatedAt,
  }) = _JoinRequest;
}
```

From lib/features/rides/data/repositories/ride_repository.dart:
```dart
class RideRepository {
  Future<Ride?> getRide(String id);
  Future<void> updateRideStatus(String rideId, String status);
  Future<void> cancelRide(String rideId);
}
```

From lib/features/rides/data/repositories/join_request_repository.dart:
```dart
class JoinRequestRepository {
  Future<String> createRequest(JoinRequest request);
  Stream<List<JoinRequest>> requestsForRide(String rideId);
  Future<void> acceptRequest(String requestId, String rideId); // Firestore transaction
  Future<void> declineRequest(String requestId);
  Future<JoinRequest?> existingRequest(String rideId, String requesterId);
}
```

From lib/features/fares/data/services/fare_calculator.dart:
```dart
class FareCalculator {
  int calculateTotalFare(String transportType, double distanceKm);
  int calculatePerPersonFare(int totalFare, int riderCount);
}
```

From lib/core/constants/app_colors.dart:
```dart
abstract final class AppColors { accent, destructive, success, onSurfaceDim, surfaceVariant, secondary }
```

From lib/features/profile/providers/profile_provider.dart:
```dart
// profileProvider -- current user's Student profile (has id, name, university, gender)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RouteVisualization widget and RideDetailScreen</name>
  <files>
    lib/features/rides/presentation/widgets/route_visualization.dart,
    lib/features/rides/presentation/screens/ride_detail_screen.dart,
    lib/features/rides/providers/ride_providers.dart
  </files>
  <read_first>
    lib/features/rides/data/models/ride.dart,
    lib/features/rides/data/models/join_request.dart,
    lib/features/rides/data/repositories/ride_repository.dart,
    lib/features/rides/data/repositories/join_request_repository.dart,
    lib/features/rides/providers/ride_providers.dart,
    lib/features/fares/data/services/fare_calculator.dart,
    lib/features/profile/providers/profile_provider.dart,
    lib/core/constants/transport_types.dart,
    lib/core/constants/app_colors.dart,
    lib/core/constants/app_spacing.dart,
    .planning/phases/02-rides-matching/02-UI-SPEC.md
  </read_first>
  <action>
1. Create `lib/features/rides/presentation/widgets/route_visualization.dart`:
   - `class RouteVisualization extends StatelessWidget`
   - Props: `String originAddress`, `String destinationAddress`, `bool compact` (default false)
   - Layout per UI-SPEC Screen 5 "Route card" section:
     - Vertical layout:
       - Origin row: 12px filled circle (accent color) + "Pick-up" caption (bodySmall, onSurfaceDim) above address text (bodyLarge)
       - Connecting line: 2px wide accent colored Container, 32px height (16px in compact mode)
       - Destination row: 12px outlined circle (accent color, 2px border) + "Drop-off" caption above address text (bodyLarge)
   - Uses Column with cross-axis alignment start

2. Add to `lib/features/rides/providers/ride_providers.dart` (append to existing file from Plan 02):
   - Provider `rideDetailProvider(String rideId)`:
     - `@riverpod` FutureProvider that calls `rideRepositoryProvider.getRide(rideId)`
   - Provider `existingJoinRequestProvider({required String rideId, required String requesterId})`:
     - `@riverpod` FutureProvider that calls `joinRequestRepositoryProvider.existingRequest(rideId, requesterId)`
   - Provider `joinRequestsForRideProvider(String rideId)`:
     - `@riverpod` StreamProvider that calls `joinRequestRepositoryProvider.requestsForRide(rideId)`
   - Function provider `sendJoinRequest(Ref ref, {required String rideId})`:
     - Reads current profile from profileProvider
     - Creates JoinRequest with requester info from profile
     - Calls `joinRequestRepositoryProvider.createRequest(request)`
   - Run build_runner after

3. Create `lib/features/rides/presentation/screens/ride_detail_screen.dart`:
   - `class RideDetailScreen extends ConsumerWidget`
   - Takes `String rideId` from GoRouter path param
   - Watches `rideDetailProvider(rideId)` for ride data
   - Layout per UI-SPEC Screen 5, scrollable:

   **App bar:**
   - Title: "Ride Details" (titleLarge)
   - Back arrow
   - Trailing: If own ride -> three-dot PopupMenuButton with "Edit" (disabled placeholder) and "Cancel Ride" (destructive, calls cancelRide with confirmation dialog per UI-SPEC copywriting: "This will remove your ride post. Riders who joined will be notified. Cancel this ride?" with "Cancel Ride" destructive + "Keep Ride" text button)

   **Section 1: Poster header (16px padding)**
   - Avatar: 56px CircleAvatar with university-colored border (3px)
   - Right: name (titleLarge), university (labelLarge, onSurfaceDim), gender icon (bodySmall, onSurfaceDim)

   **Section 2: Route card (white card, 12px radius, 16px padding)**
   - Uses RouteVisualization widget
   - Right side: mini map preview (120x80px FlutterMap, 8px radius, non-interactive) showing origin+destination pins with PolylineLayer connecting them using ride.routePolyline

   **Section 3: Ride info row (horizontal scrollable, 8px gaps)**
   - Transport chip: icon + label
   - Time chip: clock icon + formatted time
   - Seats chip: person icon + "X/Y seats"
   - Ride-hailing chip if tagged

   **Section 4: Fare estimate card (white card, 12px radius, 16px padding)**
   - "Estimated Fare" heading (labelLarge)
   - Total fare: large number (titleLarge) + "BDT" suffix (labelLarge)
   - Per person: "(~X BDT per person)" using fareCalculator.calculatePerPersonFare(ride.estimatedFare, ride.filledSeats + 1) (bodySmall, onSurfaceDim)
   - Basis: "Based on Xkm at Y BDT/km" (bodySmall, onSurfaceDim)

   **Section 5: Action area (bottom, 16px padding)**
   - Determine action state by checking: is this user's own ride? do they have an existing request? what's the request status?
   - Read local profile ID from SharedPreferences or profileProvider
   - Watch existingJoinRequestProvider(rideId: rideId, requesterId: currentUserId)

   **State logic:**
   - Own ride (ride.posterId == currentUserId): "View Requests ({count})" button (accent, full-width, 48px) -> navigates to /rides/$rideId/requests. Count from joinRequestsForRideProvider filtered to pending.
   - Not own + no existing request + not full: "Join This Ride" button (accent, full-width, 48px) -> calls sendJoinRequest, shows loading, on success updates state
   - Not own + request pending: "Request Pending" button (amber-600 bg, white text, disabled)
   - Not own + request accepted: "You're In" label (success, bodyLarge) + "Leave Ride" text button (destructive)
   - Not own + ride full: "Ride Full" label (gray-600, bodyLarge, centered)

   - Loading state for ride detail: shimmer on route card + fare card
  </action>
  <verify>
    <automated>cd /home/siam/Personal/tagme && dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -5 && dart analyze lib/features/rides/presentation/widgets/route_visualization.dart lib/features/rides/presentation/screens/ride_detail_screen.dart lib/features/rides/providers/ride_providers.dart 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class RouteVisualization" lib/features/rides/presentation/widgets/route_visualization.dart
    - grep -q "Pick-up" lib/features/rides/presentation/widgets/route_visualization.dart
    - grep -q "Drop-off" lib/features/rides/presentation/widgets/route_visualization.dart
    - grep -q "class RideDetailScreen" lib/features/rides/presentation/screens/ride_detail_screen.dart
    - grep -q "Join This Ride" lib/features/rides/presentation/screens/ride_detail_screen.dart
    - grep -q "Request Pending" lib/features/rides/presentation/screens/ride_detail_screen.dart
    - grep -q "Ride Full" lib/features/rides/presentation/screens/ride_detail_screen.dart
    - grep -q "View Requests" lib/features/rides/presentation/screens/ride_detail_screen.dart
    - grep -q "Cancel Ride" lib/features/rides/presentation/screens/ride_detail_screen.dart
    - grep -q "rideDetailProvider" lib/features/rides/providers/ride_providers.dart
    - grep -q "sendJoinRequest" lib/features/rides/providers/ride_providers.dart
  </acceptance_criteria>
  <done>
    RouteVisualization displays vertical origin-to-destination with dots and connecting line. RideDetailScreen shows full ride info with context-aware action (join/pending/accepted/full/own). Providers handle ride detail fetch, existing request check, and join request creation.
  </done>
</task>

<task type="auto">
  <name>Task 2: JoinRequestsScreen and router wiring</name>
  <files>
    lib/features/rides/presentation/screens/join_requests_screen.dart,
    lib/app/router.dart
  </files>
  <read_first>
    lib/features/rides/data/models/join_request.dart,
    lib/features/rides/data/repositories/join_request_repository.dart,
    lib/features/rides/providers/ride_providers.dart,
    lib/core/constants/app_colors.dart,
    lib/app/router.dart,
    .planning/phases/02-rides-matching/02-UI-SPEC.md
  </read_first>
  <action>
1. Create `lib/features/rides/presentation/screens/join_requests_screen.dart`:
   - `class JoinRequestsScreen extends ConsumerWidget`
   - Takes `String rideId` from GoRouter path param
   - Layout per UI-SPEC Screen 6:

   **App bar:**
   - Title: "Join Requests" (titleLarge)
   - Back arrow

   **Content: list of request cards**
   - Watches `joinRequestsForRideProvider(rideId)` stream
   - Filter to show only 'pending' status requests (accepted/declined already handled)
   - Each request card (white card, 12px radius, 16px padding, 8px vertical gaps between cards):
     - Left: requester avatar 48px CircleAvatar with university-colored border
     - Center: requester name (labelLarge) + university (bodySmall, onSurfaceDim) + request time (bodySmall, onSurfaceDim, format as relative: "5 min ago" or "2 hours ago")
     - Right: two icon buttons in Row (8px gap):
       - Accept: 40px Container with success background (green-600), circular, white check Icon (24px). semanticsLabel: "Accept [name]'s join request"
       - Decline: 40px Container with destructive background (red-600), circular, white close Icon (24px). semanticsLabel: "Decline [name]'s join request"

   **Accept interaction:**
   - On tap: show CircularProgressIndicator on button for 200ms
   - Call `joinRequestRepositoryProvider.acceptRequest(requestId, rideId)` (Firestore transaction from Plan 01)
   - On success: animate card out to right (200ms SlideTransition), show "Accepted [name]" snackbar (4 seconds, near-black bg, white text)
   - If transaction throws 'Ride is full': show "Ride is now full" snackbar, disable remaining accept buttons

   **Decline interaction:**
   - On tap: call `joinRequestRepositoryProvider.declineRequest(requestId)`
   - Animate card out to left (200ms SlideTransition)
   - No snackbar (lightweight per UI-SPEC)

   **States:**
   - Loading: 2 shimmer skeleton cards
   - Empty: centered "No requests yet. Share your ride to get co-riders." (bodyLarge)
   - When all seats filled after accepting: remaining cards show disabled state with caption "Ride is now full" below each

   - Use AnimatedList or manual animation with AnimationController for card slide-out animations

2. Update `lib/app/router.dart`:
   - Replace `/rides/:rideId` placeholder with `RideDetailScreen(rideId: state.pathParameters['rideId']!)`
   - Replace `/rides/:rideId/requests` placeholder with `JoinRequestsScreen(rideId: state.pathParameters['rideId']!)`
   - Import both screens
   - Run build_runner
  </action>
  <verify>
    <automated>cd /home/siam/Personal/tagme && dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -5 && dart analyze lib/features/rides/presentation/screens/join_requests_screen.dart lib/app/router.dart 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class JoinRequestsScreen" lib/features/rides/presentation/screens/join_requests_screen.dart
    - grep -q "Join Requests" lib/features/rides/presentation/screens/join_requests_screen.dart
    - grep -q "acceptRequest" lib/features/rides/presentation/screens/join_requests_screen.dart
    - grep -q "declineRequest" lib/features/rides/presentation/screens/join_requests_screen.dart
    - grep -q "Accepted" lib/features/rides/presentation/screens/join_requests_screen.dart
    - grep -q "No requests yet" lib/features/rides/presentation/screens/join_requests_screen.dart
    - grep -q "RideDetailScreen" lib/app/router.dart
    - grep -q "JoinRequestsScreen" lib/app/router.dart
    - grep -q "pathParameters" lib/app/router.dart
  </acceptance_criteria>
  <done>
    JoinRequestsScreen lists pending requests with accept/decline icon buttons, card slide-out animations, snackbar on accept, and full-ride handling. Router wired with RideDetailScreen and JoinRequestsScreen using rideId path parameter.
  </done>
</task>

</tasks>

<verification>
- `dart analyze lib/features/rides/` reports no errors
- RideDetailScreen shows correct action button based on ride ownership and request status
- JoinRequestsScreen accept uses Firestore transaction for atomic seat increment
- Router resolves /rides/:rideId and /rides/:rideId/requests with correct path params
- RouteVisualization used in RideDetailScreen
</verification>

<success_criteria>
- Student can view full ride details from any ride card tap
- Student can send a join request and see "Request Pending" state
- Ride poster can view all pending requests
- Ride poster can accept (atomic seat update) or decline requests
- Full rides show "Ride Full" with no join option
- Cancel ride confirmation dialog works per UI-SPEC copywriting
</success_criteria>

<output>
After completion, create `.planning/phases/02-rides-matching/02-04-SUMMARY.md`
</output>
