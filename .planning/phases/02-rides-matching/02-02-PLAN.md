---
phase: 02-rides-matching
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - lib/features/rides/providers/ride_providers.dart
  - lib/features/rides/presentation/screens/post_ride_screen.dart
  - lib/features/rides/presentation/screens/map_pin_picker_screen.dart
  - lib/features/rides/presentation/widgets/transport_selector.dart
  - lib/features/rides/presentation/widgets/seat_stepper.dart
  - lib/app/router.dart
autonomous: true
requirements: [RIDE-01]
user_setup:
  - service: openrouteservice
    why: "Route distance calculation and reverse geocoding for pin picker"
    env_vars:
      - name: ORS API Key (hardcoded constant in route_service.dart)
        source: "Sign up at https://openrouteservice.org/dev/#/signup -> Dashboard -> Request a Token"
    dashboard_config: []

must_haves:
  truths:
    - "Student can set origin by panning a map and confirming a pin location"
    - "Student can set destination by panning a map and confirming a pin location"
    - "Student can select transport type from 5 BD transport chips"
    - "Student can set departure time using Material time picker"
    - "Student can set available seats with a stepper constrained by transport capacity"
    - "Student can optionally tag a ride-hailing service"
    - "Post button is disabled until origin+destination+transport+time are set"
    - "Posting a ride calls ORS for route distance, caches it, and saves to Firestore"
  artifacts:
    - path: "lib/features/rides/presentation/screens/post_ride_screen.dart"
      provides: "Ride posting form"
      contains: "class PostRideScreen"
    - path: "lib/features/rides/presentation/screens/map_pin_picker_screen.dart"
      provides: "Full-screen map pin selector"
      contains: "class MapPinPickerScreen"
    - path: "lib/features/rides/presentation/widgets/transport_selector.dart"
      provides: "Transport type chip row"
      contains: "class TransportSelector"
    - path: "lib/features/rides/presentation/widgets/seat_stepper.dart"
      provides: "Seat count stepper"
      contains: "class SeatStepper"
    - path: "lib/features/rides/providers/ride_providers.dart"
      provides: "Riverpod providers for ride posting"
      contains: "postRide"
  key_links:
    - from: "lib/features/rides/presentation/screens/post_ride_screen.dart"
      to: "lib/features/rides/data/repositories/ride_repository.dart"
      via: "ride posting through providers"
      pattern: "rideRepositoryProvider"
    - from: "lib/features/rides/presentation/screens/map_pin_picker_screen.dart"
      to: "lib/features/rides/data/services/route_service.dart"
      via: "reverse geocoding for address display"
      pattern: "routeServiceProvider"
    - from: "lib/features/rides/presentation/screens/post_ride_screen.dart"
      to: "lib/features/rides/presentation/screens/map_pin_picker_screen.dart"
      via: "GoRouter navigation with mode param"
      pattern: "context.push.*pick-location"
---

<objective>
Build the ride posting flow: PostRideScreen form with all required fields (origin/destination map selectors, transport type chips, ride-hailing tag, time picker, seat stepper), MapPinPickerScreen for map-based location selection with reverse geocoding, and the Riverpod providers that wire posting to Firestore via the repositories from Plan 01.

Purpose: This is the primary ride creation experience. Without posting, there are no rides to search or match. Matches RIDE-01 requirement.

Output: 4 new widget/screen files, 1 providers file, router update to wire real screens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-rides-matching/02-CONTEXT.md
@.planning/phases/02-rides-matching/02-UI-SPEC.md
@.planning/phases/02-rides-matching/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must exist before this plan runs) -->

From lib/core/constants/transport_types.dart:
```dart
enum TransportType {
  rickshaw(label: 'Rickshaw', icon: Icons.electric_rickshaw, maxCapacity: 2, farePerKm: 15),
  bike(label: 'Bike', icon: Icons.pedal_bike, maxCapacity: 1, farePerKm: 10),
  bus(label: 'Bus', icon: Icons.directions_bus, maxCapacity: 10, farePerKm: 5),
  car(label: 'Car', icon: Icons.directions_car, maxCapacity: 3, farePerKm: 18),
  cng(label: 'CNG', icon: Icons.local_taxi, maxCapacity: 2, farePerKm: 12);

  static TransportType fromString(String value);
}
```

From lib/features/rides/data/models/ride.dart:
```dart
@freezed
abstract class Ride with _$Ride {
  const factory Ride({
    String? id, required String posterId, required String posterName,
    required String posterUniversity, String? posterPhotoUrl, required String posterGender,
    @JsonKey(...) GeoPoint? originGeopoint, required String originGeohash, required String originAddress,
    @JsonKey(...) GeoPoint? destinationGeopoint, required String destinationGeohash, required String destinationAddress,
    required String transportType, String? rideHailingTag, required DateTime departureTime,
    required int totalSeats, @Default(0) int filledSeats, @Default('active') String status,
    required double routeDistanceKm, @Default([]) List<List<double>> routePolyline,
    required int estimatedFare, DateTime? createdAt,
  }) = _Ride;
}
```

From lib/features/rides/data/services/route_service.dart:
```dart
class RouteService {
  Future<RouteData> getRoute(LatLng origin, LatLng destination);
  Future<String> reverseGeocode(LatLng point);
}
// Provider: routeServiceProvider
```

From lib/features/rides/data/repositories/ride_repository.dart:
```dart
class RideRepository {
  Future<String> createRide(Ride ride);
}
// Provider: rideRepositoryProvider
```

From lib/features/fares/data/services/fare_calculator.dart:
```dart
class FareCalculator {
  int calculateTotalFare(String transportType, double distanceKm);
}
// Provider: fareCalculatorProvider
```

From lib/features/profile/providers/profile_provider.dart:
```dart
// profileProvider -- provides current user's Student profile
```

From lib/app/router.dart:
```dart
// Routes to replace placeholders: /rides/post, /rides/post/pick-location
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MapPinPickerScreen and reusable widgets</name>
  <files>
    lib/features/rides/presentation/screens/map_pin_picker_screen.dart,
    lib/features/rides/presentation/widgets/transport_selector.dart,
    lib/features/rides/presentation/widgets/seat_stepper.dart
  </files>
  <read_first>
    lib/features/map/presentation/screens/map_screen.dart,
    lib/features/rides/data/services/route_service.dart,
    lib/app/theme.dart,
    lib/core/constants/app_colors.dart,
    lib/core/constants/app_spacing.dart,
    lib/core/constants/transport_types.dart,
    .planning/phases/02-rides-matching/02-UI-SPEC.md
  </read_first>
  <action>
1. Create `lib/features/rides/presentation/screens/map_pin_picker_screen.dart`:
   - `class MapPinPickerScreen extends ConsumerStatefulWidget`
   - Constructor takes `String mode` param ('origin' or 'destination') -- extracted from GoRouter query params
   - Layout per UI-SPEC Screen 4:
     - Full-screen FlutterMap (same OSM tile config as Phase 1 map_screen.dart: `TileLayer(urlTemplate: 'https://tile.openstreetmap.org/{z}/{x}/{y}.png', userAgentPackageName: 'com.tagme.app')`)
     - Initial center: user's current location via `ref.read(currentLocationProvider)` for origin mode, or the previously picked origin for destination mode (passed via GoRouter extra)
     - Zoom level 15
   - Center pin overlay:
     - `Icon(Icons.location_pin, size: 48, color: AppColors.accent)` positioned at center of screen, offset 24px above center so pin tip touches center point
     - Subtle drop shadow below pin (Container with BoxShadow)
   - Top overlay:
     - Mode label: "Set Pick-up Point" or "Set Drop-off Point" per mode param
     - Positioned 16px below SafeArea top, center-aligned
     - White text with drop shadow (same pattern as MapTopBar)
   - Bottom overlay:
     - White card anchored to bottom (16px margin, 12px radius, 16px padding)
     - Address text: shows reverse-geocoded address of current map center
     - "Confirm Location" button: accent, full-width, 48px height, 12px radius
   - Floating back arrow: top-left, over map
   - Interaction:
     - Listen to `MapController.mapEventStream` or `onPositionChanged` callback for map pans
     - Debounce 500ms: after map stops moving, call `routeService.reverseGeocode(currentCenter)` to update address
     - While geocoding: show shimmer on address text
     - On "Confirm Location" tap: pop with result `{'lat': lat, 'lng': lng, 'address': addressString}` using `context.pop(result)`
   - Use `MapController` for programmatic map control
   - Import geolocator for user location, flutter_map for map

2. Create `lib/features/rides/presentation/widgets/transport_selector.dart`:
   - `class TransportSelector extends StatelessWidget`
   - Props: `TransportType? selected`, `ValueChanged<TransportType> onSelected`
   - Layout per UI-SPEC Screen 3 section 3:
     - Horizontal Row of 5 selectable chips with 8px gaps (use `Wrap` with 8px spacing for <360px fallback)
     - Each chip: Column with icon (24px) + label below (caption style), 64px wide, 56px height
     - Selected state: accent background, white icon + text
     - Unselected state: surfaceVariant background, gray-600 icon, on-surface text
     - Single selection (radio behavior): tapping selected chip does nothing, tapping unselected selects it
   - Iterate `TransportType.values` to build chips dynamically
   - Touch target: 44px minimum (W3C per UI-SPEC)

3. Create `lib/features/rides/presentation/widgets/seat_stepper.dart`:
   - `class SeatStepper extends StatelessWidget`
   - Props: `int value`, `int min` (default 1), `int max`, `ValueChanged<int> onChanged`
   - Layout per UI-SPEC Screen 3 section 6:
     - Row: minus button (32px circle, surfaceVariant) | number (body style, 32px width, centered) | plus button (32px circle, surfaceVariant)
     - Minus disabled at min value (reduced opacity), plus disabled at max
     - Tapping buttons calls onChanged with value-1 or value+1
   - Accessibility: semanticsLabel "Available seats: [count]. Increase. Decrease."
  </action>
  <verify>
    <automated>cd /home/siam/Personal/tagme && dart analyze lib/features/rides/presentation/screens/map_pin_picker_screen.dart lib/features/rides/presentation/widgets/transport_selector.dart lib/features/rides/presentation/widgets/seat_stepper.dart 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class MapPinPickerScreen" lib/features/rides/presentation/screens/map_pin_picker_screen.dart
    - grep -q "reverseGeocode" lib/features/rides/presentation/screens/map_pin_picker_screen.dart
    - grep -q "Confirm Location" lib/features/rides/presentation/screens/map_pin_picker_screen.dart
    - grep -q "context.pop" lib/features/rides/presentation/screens/map_pin_picker_screen.dart
    - grep -q "class TransportSelector" lib/features/rides/presentation/widgets/transport_selector.dart
    - grep -q "TransportType" lib/features/rides/presentation/widgets/transport_selector.dart
    - grep -q "class SeatStepper" lib/features/rides/presentation/widgets/seat_stepper.dart
    - grep -q "onChanged" lib/features/rides/presentation/widgets/seat_stepper.dart
  </acceptance_criteria>
  <done>
    MapPinPickerScreen shows full-screen map with center pin, reverse geocoding (debounced 500ms), and "Confirm Location" button that pops with coordinates+address. TransportSelector renders 5 BD transport chips with radio selection. SeatStepper provides increment/decrement constrained by min/max.
  </done>
</task>

<task type="auto">
  <name>Task 2: PostRideScreen, providers, and router wiring</name>
  <files>
    lib/features/rides/providers/ride_providers.dart,
    lib/features/rides/presentation/screens/post_ride_screen.dart,
    lib/app/router.dart
  </files>
  <read_first>
    lib/features/rides/presentation/screens/map_pin_picker_screen.dart,
    lib/features/rides/presentation/widgets/transport_selector.dart,
    lib/features/rides/presentation/widgets/seat_stepper.dart,
    lib/features/rides/data/repositories/ride_repository.dart,
    lib/features/rides/data/services/route_service.dart,
    lib/features/fares/data/services/fare_calculator.dart,
    lib/features/profile/providers/profile_provider.dart,
    lib/app/router.dart,
    .planning/phases/02-rides-matching/02-UI-SPEC.md
  </read_first>
  <action>
1. Create `lib/features/rides/providers/ride_providers.dart`:
   - Import all ride repositories, services, profile provider
   - Add `@riverpod` annotation and part directive for code generation
   - Provider `Future<String> postRide(Ref ref, {...params...})`:
     - Takes: originLat, originLng, originAddress, destLat, destLng, destAddress, transportType, rideHailingTag (nullable), departureTime, totalSeats
     - Reads current profile from `profileProvider` to get posterName, posterUniversity, posterGender, posterPhotoUrl, posterId
     - Calls `routeServiceProvider` to get route (distance + polyline) from origin to destination
     - Calls `fareCalculatorProvider` to calculate estimated fare
     - Creates Ride object with all fields, including GeoFirePoint-encoded geohash for origin and destination
     - Calls `rideRepositoryProvider.createRide(ride)`
     - Returns ride document ID
   - Use GeoFirePoint from geoflutterfire_plus to compute geohash: `GeoFirePoint(GeoPoint(lat, lng)).geohash`
   - Run build_runner after creating

2. Create `lib/features/rides/presentation/screens/post_ride_screen.dart`:
   - `class PostRideScreen extends ConsumerStatefulWidget`
   - Layout per UI-SPEC Screen 3, full-screen scrollable form:
   - App bar: "Post a Ride" title, back arrow
   - Content sections with 24px spacing, 32px horizontal padding:

   **Section 1: Origin selector ("Pick-up Point")**
   - Label: "Pick-up Point" (labelLarge, onSurfaceDim)
   - Tap area: 48px height, surfaceVariant bg, 12px radius
   - Default: "Tap to set on map" placeholder
   - Selected: shows address text + small 64px map preview thumbnail
   - Tap: `context.push('/rides/post/pick-location?mode=origin')` -- awaits result with `GoRouterHelper.push` returning a Future, or use GoRouter `extra` to pass callback
   - NOTE: Use `context.push<Map<String, dynamic>>('/rides/post/pick-location?mode=origin')` and await the result. The MapPinPickerScreen pops with the result map.

   **Section 2: Destination selector ("Drop-off Point")**
   - Same layout as origin, label: "Drop-off Point"
   - Tap: `context.push<Map<String, dynamic>>('/rides/post/pick-location?mode=destination')`

   **Section 3: Transport type selector**
   - Label: "Transport Type" (labelLarge, onSurfaceDim)
   - Uses TransportSelector widget from Task 1
   - On selection change: updates seat count max to selected transport's maxCapacity, resets seats to maxCapacity - 1

   **Section 4: Ride-hailing tag (optional)**
   - Label: "Ride-Hailing Service (optional)" (labelLarge, onSurfaceDim)
   - 3 chips in a row: Pathao, Uber, Obhai
   - Toggle behavior (can deselect): selected shows accent outline + accent text, unselected shows surfaceVariant bg
   - State: nullable string

   **Section 5: Departure time**
   - Label: "Departure Time" (labelLarge, onSurfaceDim)
   - Tap area: shows "Select time" placeholder or formatted time
   - Tap: `showTimePicker(context: context, initialTime: TimeOfDay.now())`
   - Selected: shows "Today, 8:30 AM" format using intl DateFormat
   - If time is in past: auto-bump to tomorrow, show "Tomorrow" caption below

   **Section 6: Available seats**
   - Label: "Available Seats" (labelLarge, onSurfaceDim)
   - Uses SeatStepper with min=1, max=transport.maxCapacity
   - Default: maxCapacity - 1 (poster takes one seat)
   - Bike: auto-set to 1, stepper disabled (max=1)

   **Section 7: Post button**
   - "Post Ride" button: accent, full-width, 48px height, 12px radius
   - Disabled (38% opacity) until origin + destination + transport + time are all set
   - Loading state: CircularProgressIndicator replacing text
   - On tap: calls postRide provider, on success shows "Ride posted successfully" snackbar and pops back

   - Local state management: use StatefulWidget state for form fields (origin, destination, transport, tag, time, seats, isPosting)

3. Update `lib/app/router.dart`:
   - Replace `/rides/post` placeholder with `PostRideScreen()`
   - Replace `/rides/post/pick-location` placeholder with `MapPinPickerScreen(mode: state.uri.queryParameters['mode'] ?? 'origin')`
   - Import both new screens
   - Run build_runner to regenerate router.g.dart
  </action>
  <verify>
    <automated>cd /home/siam/Personal/tagme && dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -5 && dart analyze lib/features/rides/providers/ride_providers.dart lib/features/rides/presentation/screens/post_ride_screen.dart lib/app/router.dart 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class PostRideScreen" lib/features/rides/presentation/screens/post_ride_screen.dart
    - grep -q "Post a Ride" lib/features/rides/presentation/screens/post_ride_screen.dart
    - grep -q "Pick-up Point" lib/features/rides/presentation/screens/post_ride_screen.dart
    - grep -q "Drop-off Point" lib/features/rides/presentation/screens/post_ride_screen.dart
    - grep -q "TransportSelector" lib/features/rides/presentation/screens/post_ride_screen.dart
    - grep -q "SeatStepper" lib/features/rides/presentation/screens/post_ride_screen.dart
    - grep -q "showTimePicker" lib/features/rides/presentation/screens/post_ride_screen.dart
    - grep -q "Ride-Hailing" lib/features/rides/presentation/screens/post_ride_screen.dart
    - grep -q "postRide" lib/features/rides/providers/ride_providers.dart
    - grep -q "PostRideScreen" lib/app/router.dart
    - grep -q "MapPinPickerScreen" lib/app/router.dart
  </acceptance_criteria>
  <done>
    PostRideScreen presents a scrollable form with all 7 sections per UI-SPEC. MapPinPickerScreen integrated for origin/destination selection. Post button disabled until all required fields set. Posting calculates route distance via ORS, computes fare, and saves ride to Firestore. Router wired with real screens replacing placeholders.
  </done>
</task>

</tasks>

<verification>
- `dart analyze lib/features/rides/` reports no errors
- PostRideScreen imports and uses TransportSelector, SeatStepper, and MapPinPickerScreen
- Router correctly routes to PostRideScreen and MapPinPickerScreen
- PostRide provider chains: profile -> route service -> fare calculator -> ride repository
</verification>

<success_criteria>
- Student can navigate to Post Ride screen from FAB (once RidesTabScreen exists in Plan 03)
- Student can select origin and destination on full-screen map with reverse geocoding
- Student can select transport type, optionally tag ride-hailing, set time, set seats
- Post button creates ride in Firestore with cached ORS route data and fare estimate
</success_criteria>

<output>
After completion, create `.planning/phases/02-rides-matching/02-02-SUMMARY.md`
</output>
